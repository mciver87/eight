{#
/**
 * @file
 * Default simple view template to display a list of rows.
 *
 * @ingroup views_templates
 */#}
{% if title is not empty %}
  <h3>{{ title }}</h3>
{% endif %}
<ul>
{# PHP: foreach ($rows as $id => $row): #}
  <li{% if classes_array  %} {# PHP:print ' class="' . $classes_array[$id] .'"';#}  {% endif %}>
    {{ row }}
  </li>
{% endfor %}
</ul>